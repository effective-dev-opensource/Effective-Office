# Gradle properties
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Kotlin properties
kotlin.code.style=official
kotlin.incremental=true
kotlin.daemon.jvmargs=-Xmx6144M

# Project properties
group=band.effective.office
version=1.0.1

android.useAndroidX=true

# Вариант сборки: по умолчанию upstream (Android/iOS).
# Aurora-вариант (только clients/tablet) включается флагом `-PbuildVariant=aurora`.
# buildVariant=aurora