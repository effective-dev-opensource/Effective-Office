<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only overlay: allow plaintext HTTP so the debug build can reach the
     local backend at http://10.0.2.2:8080 from the Android emulator. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
