package band.effective.office.tablet.navigation

import kotlinx.serialization.Serializable

/** Settings screen — the start destination until a room is configured. */
@Serializable
object SettingsRoute

/** Room dashboard — the start destination once a room is configured. */
@Serializable
object MainRoute
